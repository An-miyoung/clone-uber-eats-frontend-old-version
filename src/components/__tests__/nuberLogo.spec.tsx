import { render } from "@testing-library/react";
import React from "react";
import NuberLogo from "../nuberLogo";

describe("<NuberLogo />", () => {
  it("renders OK", () => {
    render(<NuberLogo />);
  });
});
